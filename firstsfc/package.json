{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",  // <-- Add this line
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    // other dependencies here
  },
  "devDependencies": {
    "vite": "^latest-version"
  }
}